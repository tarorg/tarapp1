import { Text, View } from "react-native";

export default function Sales() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
        backgroundColor: "#FFFFFF",
      }}
    >
      <Text style={{ fontSize: 32, fontWeight: 'bold' }}>Sales</Text>
    </View>
  );
}
